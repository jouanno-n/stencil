{
  "name": "styles",
  "version": "1.0.0",
  "scripts": {
    "start": "sass scss:dist -w",
    "build": "sass --no-source-map scss:dist -s compressed",
    "build:dev": "sass scss:dist",
    "doc": "pnpm build:dev && ts-node tools/css-docs.ts"
  },
  "files": [
    "dist",
    "scss"
  ],
  "devDependencies": {
    "postcss": "8.5.6",
    "sass": "1.94.2"
  }
}
